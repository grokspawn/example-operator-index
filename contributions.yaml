schema: olm.composite
components:
  - name: v4.10
    destination:
      path: rhbk-operator
    strategy:
      name: semver
      template:
        schema: olm.builder.semver
        config:
          input: semver-template.yaml
          output: catalog.yaml
  - name: v4.11
    destination:
      path: rhbk-operator
    strategy:
      name: semver
      template:
        schema: olm.builder.semver
        config:
          input: semver-template.yaml
          output: catalog.yaml
  - name: v4.12
    destination:
      path: rhbk-operator
    strategy:
      name: semver
      template:
        schema: olm.builder.semver
        config:
          input: semver-template.yaml
          output: catalog.yaml
  - name: v4.13
    destination:
      path: rhbk-operator
    strategy:
      name: semver
      template:
        schema: olm.builder.semver
        config:
          input: semver-template.yaml
          output: catalog.yaml
  - name: v4.14
    destination:
      path: rhbk-operator
    strategy:
      name: semver
      template:
        schema: olm.builder.semver
        config:
          input: semver-template.yaml
          output: catalog.yaml
  - name: v4.15
    destination:
      path: rhbk-operator
    strategy:
      name: semver
      template:
        schema: olm.builder.semver
        config:
          input: semver-template.yaml
          output: catalog.yaml
